const {
	SlashCommandBuilder,
	Client,
	Collection,
	GatewayIntentBits,
	Routes,
} = require('discord.js');

const { REST } = require('@discordjs/rest');

module.exports = {
	SlashCommandBuilder,
	Client,
	Collection,
	GatewayIntentBits,
	Routes,
	REST,
};
